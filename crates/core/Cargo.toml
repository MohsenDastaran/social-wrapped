[package]
name = "social-wrapped-core"
version.workspace = true
edition.workspace = true
description = "Shared business logic for social-wrapped"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
