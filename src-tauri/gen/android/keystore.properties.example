# Copy to keystore.properties (gitignored) for local signed release APKs.
# Default CI sideload key — replace with your own for Play Store uploads.
# storeFile is resolved from the app/ Gradle module → use ../ for gen/android/.
password=social-wrapped-release
keyAlias=upload
storeFile=../ci-release.jks
